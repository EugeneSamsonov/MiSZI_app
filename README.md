## [MiSZI_app](#-miszi_app--веб-приложение-для-изучения-алгоритмов-шифрования).
## [Secure Share](#secure-share--сервис-безопасного-обмена-файлами).

# 🧩 **MiSZI_app — веб-приложение для изучения алгоритмов шифрования**

**MiSZI_app** — это учебное веб-приложение, предназначенное для изучения синхронных и асинхронных алгоритмов шифрования данных. Проект включает в себя интерактивную систему тестирования, теоретические материалы, систему ролей и административную панель для управления контентом и пользователями.

---

## 📚 **Возможности проекта**

### 🔐 Аутентификация и безопасность

* Регистрация и вход в систему
* Ролевая система (пользователь / администратор)
* Смена пароля с усиленной валидацией
* Возможность блокировки пользователей
* Защита от брут-форса с помощью **django-axes**

---

### 📊 Система тестирования

* Тесты, сгруппированные по категориям
* Создание и редактирование тестов администраторами
* Вопросы с одним и несколькими правильными ответами
* Ограничение количества попыток
* Автоматическая проверка и оценивание
* Просмотр истории попыток и статистики
* Привязка тестов к теоретическим материалам

---

### 📘 Теоретические материалы

* Категории и подразделы теории
* Подробные статьи по алгоритмам шифрования
* Связь теории с тестовыми заданиями
* Удобная структура для изучения

---

### ⚙️ Административная панель

* Управление пользователями (добавление, блокировка, изменение ролей)
* Создание, редактирование и удаление тестов
* Управление теоретическими материалами
* Просмотр статистики прохождения тестов
* Управление ролями и суперпользователями

---

## 🏗️ **Технологический стек**

| Компонент             | Использование                                          |
| --------------------- | ------------------------------------------------------ |
| **Backend Framework** | Django 5.2                                             |
| **База данных**       | SQLite3                                                |
| **Frontend**          | HTML, CSS, JavaScript                                  |
| **Безопасность**      | django-axes                                            |
| **Контейнеризация**   | Docker                                                 |


---

## 📂 **Структура приложения**

```
MiSZI_app/
│
├── users/         # Управление пользователями и аутентификация
├── study_tests/   # Система тестирования
├── theory/        # Теоретические материалы
├── app/           # Главные настройки проекта
└── fixtures/      # Начальные данные
```

---

## 🚀 **Установка и запуск**

### **Способ 1. Docker (рекомендуется)**

```bash
# Сборка и запуск контейнера
docker-compose up --build
```

Загрузка фикстур:

```bash
docker exec -it MiSZI_app sh -c "python3 manage.py loaddata fixtures/data.json"
```

---

### **Способ 2. Локальная установка**

```bash
# Клонирование репозитория
git clone https://github.com/EugeneSamsonov/MiSZI_app.git
cd MiSZI_app

# Установка зависимостей
pip install -r requirements.txt

# Применение миграций
python manage.py migrate

# Запуск сервера
python manage.py runserver 0.0.0.0:8000
```

---

## 🛠️ **Команды управления**

```bash
# Сброс всех блокировок django-axes
python manage.py axes_reset

# Сброс блокировок по IP
python manage.py axes_reset_ip [ip ...]

# Сброс блокировок по имени пользователя
python manage.py axes_reset_username [username ...]

# Создание суперпользователя
python manage.py createsuperuser
```

---

## 🎯 **Для кого предназначен MiSZI**

* Студентов, изучающих криптографию
* Преподавателей дисциплин, связанных с шифрованием
* Людей, интересующихся защитой данных
* Разработчиков, которым требуется демонстрационное учебное приложение

---

# Secure Share — сервис безопасного обмена файлами

Secure Share — учебный веб-сервис, предназначенный для безопасной передачи файлов по временным и ограниченным ссылкам.  
Проект ориентирован не на пользовательский интерфейс, а на корректную реализацию механизмов контроля доступа, защиты хранения файлов и управления сроком жизни ссылок.

Сервис разработан в рамках учебного задания по дисциплине  
**«Методы и средства защиты информации»**.

---

## Назначение и функции веб-сайта

Secure Share решает задачу передачи файлов между пользователями без предоставления прямого доступа к серверному хранилищу. Пользователь взаимодействует с системой исключительно через веб-интерфейс и защищённые ссылки, не имея информации о реальных путях хранения файлов.

Основные функции веб-сайта:
- загрузка файлов с серверной валидацией;
- безопасное хранение файлов без использования оригинальных имён;
- создание временных и ограниченных ссылок на скачивание;
- контроль количества скачиваний и срока действия ссылок;
- управление файлами и ссылками исключительно их владельцем;
- автоматическая деактивация ссылок при нарушении условий доступа.

---

## Инструкция по использованию

### Доступ к сайту и авторизация

Для перехода к основному функционалу веб-сайта необходимо в адресной строке браузера указать путь: /sshare


##### Доступ к функционалу возможен только для авторизованных пользователей.  
Перед началом работы пользователь должен зарегистрироваться или войти в существующую учётную запись.

При регистрации указываются:
- имя;
- фамилия;
- логин;
- пароль;
- подтверждение пароля.

Пароль должен удовлетворять повышенным требованиям безопасности и включать:
- символы кириллицы;
- символы латиницы;
- цифры;
- математические операции (например 1+1).

Во время авторизации, при вводе неверного пароля более **5 раз подряд** система автоматически блокирует доступ по IP-адресу пользователя, что защищает сервис от атак перебора паролей (brute force).

---

### Загрузка и управление файлами

После авторизации пользователь получает доступ к главной странице, где может загрузить файл через веб-интерфейс.  
При загрузке файл проходит серверную проверку на допустимый размер, тип и корректность имени.

Загруженные файлы отображаются в личном кабинете пользователя. Каждый файл доступен только его владельцу и может быть помечен как удалённый (мягкое удаление) без немедленного физического удаления с сервера.

---

### Создание и использование ссылок

Для каждого файла пользователь может создать одну или несколько ссылок для скачивания.  
Ссылка может иметь ограничение по времени действия и/или по количеству скачиваний.

Для скачивания файла по уже переданной ссылке необходимо в адресной строке браузера указать путь вида: ```sshare/download/token```


где `<token>` — уникальный идентификатор ссылки, и нажать Enter.  
Если условия доступа соблюдены, файл автоматически начнёт скачивание.

---

## Алгоритмы работы сервиса

Алгоритм загрузки файла:
1. пользователь отправляет файл через веб-форму;
2. сервер выполняет валидацию размера, типа и имени файла;
3. оригинальное имя сохраняется в базе данных;
4. файл сохраняется на сервере под UUID-именем;
5. файл привязывается к пользователю-владельцу.

Алгоритм работы ссылки на скачивание:
1. пользователь создаёт ссылку с заданными ограничениями;
2. система генерирует уникальный UUID-токен;
3. при обращении по ссылке сервер проверяет:
   - существование ссылки;
   - активность ссылки;
   - состояние файла;
   - срок действия;
   - количество предыдущих скачиваний;
4. при успешной проверке файл отдаётся пользователю;
5. при достижении лимита или истечении времени ссылка автоматически деактивируется.

---

## Алгоритмы и методы защиты

### Защита, реализованная на уровне Django

Фреймворк Django по умолчанию нейтрализует ряд распространённых веб-уязвимостей, в том числе:

- **SQL Injection** — использование ORM и параметризованных запросов;
- **Cross-Site Scripting (XSS)** — автоматическое экранирование данных в шаблонах;
- **Cross-Site Request Forgery (CSRF)** — защита через CSRF-токены;
- **Clickjacking** — защита с помощью HTTP-заголовков;
- **Host Header Injection** — проверка допустимых хостов;
- **Command Injection** — отсутствие прямого выполнения пользовательских команд;
- **Path Traversal** — строгая обработка имён файлов и путей;
- **Session Hijacking** — защищённые cookies и серверное хранение сессий.

---

### Защита данных пользователей и файлов

Имена пользователей, файлы и ссылки не используются в явном виде для доступа к данным.  
На сервере:

- файлы хранятся под UUID, а не под оригинальными именами;
- ссылки представлены UUID-токенами;
- реальные пути хранения файлов скрыты от клиента;
- доступ осуществляется только через серверную логику.

Таким образом, даже при знании структуры проекта невозможно получить файл без действительной ссылки.

---

### Контроль доступа и ограничение действий

В проекте реализован строгий контроль прав доступа:
- пользователь может работать только со своими файлами;
- управление ссылками доступно только владельцу файла;
- попытки доступа к чужим объектам приводят к ошибке `403 Forbidden`.

Дополнительно реализованы:
- ограничение количества попыток входа;
- блокировка по IP-адресу;
- отсутствие раскрытия причин отказа в доступе при скачивании файлов.

---

## Используемые технологии

Backend: Django  
Frontend: HTML, CSS, JavaScript  
Хранение файлов: файловая система сервера  
База данных: SQLite (по умолчанию)

---



